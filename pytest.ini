[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Skip real-LLM tests unless explicitly requested: pytest -m llm
addopts = -m "not llm"

# Custom marks
markers =
    llm: tests that call a real LLM (require API key, skip by default)
